package chapter_02.design_principles.creational_design_patterns.factory_pattern;

/**
 * Created by Syed Wajahat on 12/13/2019.
 */
public class MyClass {
    /*
    What if we do not know the precise type of the object to be created at run time
    We to have loosely couple the classes
    Factory pattern supports polymorphism
    An object in memory can be accessed through a reference of the same type
    or a reference of the parent class or a reference of an interface
    that is implemented by the class of that object
     */

    /*
    Create an abstract class food, then create food types (classes)
    from the Food class then create a FoodFactory to return objects of different types
    on runtime using polymorphism
     */
}
